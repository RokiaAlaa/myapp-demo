apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  name: myapp

spec:
  selector:
    app: myapp

  ports:
  - port: 8000
    targetPort: 8000

  type: ClusterIP
